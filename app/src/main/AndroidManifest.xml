<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.harshpatel.ministatll">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cv_input_window" />
        <activity android:name=".swv_input_window" />
        <activity android:name=".dpv_input_window" />
        <activity android:name=".acv_input_window" />
        <activity android:name=".chronoamperometry_input_window" />
        <activity android:name=".pv_input_window" />
        <activity android:name=".graph_example"></activity>
    </application>

</manifest>